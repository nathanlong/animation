<!DOCTYPE html>
<html class="no-js" lang="">
	<head>
		<meta charset="utf-8" />
		<title>{{ title }}</title>
		<meta name="description" content="" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="stylesheet" href="{{ '/css/common.css' | url }}">
	</head>

	<body>
		<main id="content">
			<code class="breadcrumbs">
				<a href="{{ '/' | url }}">&larr; Animation</a> / <a href="{{ '../ '}}">Thoughts</a>
			</code>
			<div class="container">
				<h1><code class="text-gray">{{ number }}:</code> {{ title }}</h1>
				{{ content | safe }}
			</div>
		</main>
	</body>
</html>
